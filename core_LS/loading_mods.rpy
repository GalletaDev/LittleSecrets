


#init -11 python:
    #import os
    #import renpy

    #mods_folder = renpy.exports.config.gamedir + "/mods"

    #if not os.path.exists(mods_folder):
        #os.makedirs(mods_folder)
    # Listar todos los archivos .rpy dentro de mods
    #mods_files = []

    #if os.path.isdir(mods_folder):
        #for f in os.listdir(mods_folder):
            #if f.endswith(".rpy"):
                #mods_files.append(os.path.join(mods_folder, f))

    # Cargar cada archivo mod
    #for mod_path in mods_files:
        #renpy.exports.rpy_execute_file(mod_path)

